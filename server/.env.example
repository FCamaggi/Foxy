# Backend Environment Variables
# Copia este archivo a .env y actualiza las variables

# Puerto del servidor
PORT=3001

# MongoDB Atlas Connection String
# 1. Crea una cuenta en https://www.mongodb.com/cloud/atlas
# 2. Crea un cluster gratuito (M0)
# 3. Crea un usuario de base de datos
# 4. Obtén el connection string y reemplaza <username>, <password> y <cluster>
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/foxy?retryWrites=true&w=majority

# CORS Origin - URL del frontend
# Desarrollo: http://localhost:3000
# Producción: URL de tu sitio en Netlify
CORS_ORIGIN=http://localhost:3000

# Entorno
NODE_ENV=development
